# AGENTS.md — RAG REDO 项目协作规范

> **[强制] AGENTS.md ⇄ CLAUDE.md 双文件同步规则**（沿用自参考模板项目验证过的做法）
> 本文件有两个内容必须逐字一致的副本：`AGENTS.md`（供 Codex/AGENTS 体系读取）和 `CLAUDE.md`（供 Claude Code 体系读取）。任何 AI 改动本文件时，必须同时把完全相同的改动写入另一个文件，并在同一次 commit 里提交；改动前先读另一个文件确认没有未同步的差异。两个文件只在这条同步说明块里各自说明"自己是哪一个"，其余正文必须完全相同。
>
> **这份文件是项目规范，不是 TODO/进度记录。** 所有约束、理念、硬性规则都直接写在这里——AI 只读这一份文件也应该能做出不违反架构的改动，不应该必须跳转到别的文档才能知道"这样做行不行"。`docs/` 下的其他文件（ARCHITECTURE.md/PLUGIN_SPEC.md/DATA_FLOW.md）是同一批规则的展开版（更多例子、更多背景、表格），不是"规则实际写在那边、这里只放链接"的关系；两边如果出现矛盾，以这份文件为准，发现后要立刻回来修。进度/路线图记录在 `docs/ROADMAP.md`，历史记录在 `docs/legacy/`，都不在这份文件里。

## 项目一句话

Obsidian 笔记本地语义检索系统的完全重构：插件化架构，核心只有插件运行时+数据流管理器两个组件，其余一切（检索算法、embedding 模型、OCR、GUI、MCP 工具）都是插件。目标用户包含完全不懂编程的人，安装必须是"下载→双击→能用"。

## 当前状态

**Phase 1（文字检索 MVP）核心链路已实现（2026-09-23）**：`core/` 两个核心组件 + Agent 写权限门禁全部落地；14 个官方插件（提取 md/txt/pdf文字层/docx、切块、库管理、BM25、BGE-M3、Chroma、RRF、重排、近似去重、MCP 服务、GUI 壳、导出/导入归档）+ `core/pipeline.py` 编排层，227 用例全绿（`tests/run.py`），另有真实子进程 MCP 协议冒烟、真实 pywebview 渲染冒烟、真实 demo-vault 索引验证。导出/导入（`official-import-export`）让一个库能带着已建索引搬到另一台机器，不需要重新跑一遍索引。未完成：Phase 2（OCR/视觉插件，需要真实 API Key 或大模型下载，当前环境未配置）、Phase 3 剩余项（库 AI 摘要、Agent 写权限门禁通用化）、Phase 4（Windows 安装包，需要真实 Windows 环境构建）。逐项完成情况见 [docs/ROADMAP.md](docs/ROADMAP.md)。

## 核心哲学：五条约束

1. **安装极简化**：目标用户不懂编程。下载→双击→能用，不允许要求用户手动装 Python/CUDA/Node 之类的环境或敲命令行。
2. **架构插件化，核心只有两个组件**：插件运行时 + 数据流/契约管理器（见下节）。除此之外的一切——用哪个 embedding 模型、支不支持 OCR、要不要页面级视觉检索、用哪个 GUI——都是插件，理论上都可以被换掉。
3. **数据流统一管控**：数据怎么流、格式是什么，只能有一处权威定义，不能"哪里方便就从哪接一根线"。
4. **依赖关系独立，且不污染主机环境**：不要求用户预装任何运行环境；反过来，软件本身也不能在用户机器上留下自己隔离目录之外的痕迹（全局 Python 环境、全局 PATH、注册表、孤儿进程）。这是双向的——"不依赖主机"和"不污染主机"缺一不可，详见下面"依赖独立与主机环境隔离"一节。
5. **Windows 优先，Linux 能开发**：Windows 是最终用户的主战场，深度适配是硬性要求；Linux 用于日常开发，不要求打包分发。

## 两个核心组件

### 插件运行时（Plugin Runtime）

负责插件的发现、校验、加载、生命周期、以及跨插件的资源仲裁：

- **发现**：扫描 `plugins/*/plugin.toml`，是**显式触发**的（启动时一次，或用户点"重新扫描"），不做持续文件监听（避免半成品文件夹被提前加载）。**已发现的插件之间启用/禁用是即时的，不需要重启核心**。
- **校验**：manifest 格式、`api_version` 兼容性、`requires` 依赖是否满足。任何一项不过，插件停在 `invalid` 状态，原因可见，不做静默降级。
- **生命周期**：`on_load → on_enable`，禁用/卸载走 `on_disable → on_unload`，全程在 try/except 边界内——一个插件炸了只标记它 `failed`，绝不牵连其他插件或拖垮宿主进程。
- **扩展点注册表**：插件在 manifest 声明 `provides`。单例型扩展点（`embedder`/`vector_store`/`lexical_index`/`fusion`/`reranker`/`llm_provider` 等，同一时刻只能有一个生效实现）由配置显式指定当前用哪个；多值型扩展点（`extractor:*`/`gui_panel`/`mcp_tool_provider`/`dedup`/`visual_index` 等，可以同时存在多个）由所有已启用插件一起参与，核心聚合。
- **资源仲裁器**：通用的、不懂任何 RAG 领域知识的"具名资源租约"机制（获取/抢占/空闲释放/失败开放），插件（典型如 `subprocess_service` 形态、需要独占 GPU 的插件）用它申请像 `gpu:0` 这样的资源锁。这是核心服务，不是插件——两个互不知情的插件没法公平抢一块物理资源，需要一个中立裁判。

### 数据流 / 契约管理器（Data Flow & Contract Manager）

负责"数据从哪来、经过谁、到哪去"的强制单向管道，以及插件唯一被允许读写持久化状态的入口：

- 索引态管道方向固定：发现→抽取→切块→向量化/词法化→写入→后处理，没有反向箭头。查询态：解析→检索→融合→重排→标注→响应。
- 插件不直接开 Chroma/SQLite/JSON 文件，一律通过这个组件暴露的 DataStore API，按自己声明的扩展点类型拿到收窄过的读写权限。
- **Agent 写权限门禁**是这里提供的服务：提案号+确认码+TTL+一次性的两段式确认模式，任何插件要接受 AI 触发的写操作都直接复用这一套，不用各自发明。这也是核心服务而非插件——写权限的裁决必须有唯一入口，不能每个插件自己决定"这算不算 AI 写"。

## 插件规则

- 目录结构、`plugin.toml` 字段、生命周期时序图的完整定义见 [docs/PLUGIN_SPEC.md](docs/PLUGIN_SPEC.md)——这里只强调贯穿全局的硬性要求。
- **运行时形态**：`in_process`（纯 Python、轻量、不易冲突）或 `subprocess_service`（重依赖/独立环境/需要独占资源，自带 venv，和核心走本机 HTTP 或 stdio JSON-RPC）。判断标准：会不会和核心或其他插件的依赖冲突、需不需要独占 GPU、依赖体积是否大到不该塞进默认安装包。
- **模型/Provider 类扩展点必须支持运行时切换，这是插件化最核心的承诺之一，不能因为省事而退化成"装完就焊死"**：`embedder`/`reranker`/`llm_provider` 这类单例扩展点，只要多个候选实现已经装好，切换用哪个纯粹是改"当前激活哪个"的配置，不需要重启、不需要重新走安装流程。`llm_provider` 额外考虑参考模板项目验证过的"有序回退链"模式（优先用一个，不可用自动试下一个，而不是每个插件各自维护一套可用性探测逻辑），具体在 Phase 3 落地库摘要/HyDE 类功能时细化。
- 换了模型/embedding 版本后，旧索引数据要能判断"过期需要重算"——复用版本号机制（见 [docs/LESSONS.md](docs/LESSONS.md) 第3条），不能让用户手动发现"结果好像不对了"才回头查。

## 数据流铁律（禁止事项，完整版见 DATA_FLOW.md）

1. 插件之间不能互相 import 或直接调用对方的代码，只能通过核心编排的管道交换数据。
2. GUI 插件不能抄近道——只能调用核心暴露的查询/命令 API，不能直接连数据库或直接调某个具体插件的内部实现。
3. 跨插件传递的数据格式只能用核心 `contracts` 里定义好的类型，不能私下发明字段名口头约定。
4. **同一件事的标准答案只能在一处定义**（比如"一个文件算不算在检索范围内"只在 `library_manager` 插件里判定一次），其他插件只能查询结果，不能各自维护一份影子逻辑——继承旧项目 `library.decide_included`"裁决唯一实现"的教训。
5. 插件不直接读写持久化存储，一律通过 DataStore API，按扩展点类型收窄权限。

## 架构红线（违反=架构事故，不是风格问题）

1. 插件之间不能互相直接调用/import 彼此的实现——只能通过核心编排的数据流管道交换数据。
2. 同一件事的判定逻辑只能有一处权威实现，其他地方只能查询结果。
3. 插件不直接读写持久化存储，一律通过 DataStore API。
4. 插件的失败必须被隔离折叠，绝不能让一个插件的异常拖垮宿主进程或牵连其他插件——继承旧项目"extractor 绝不抛异常"的教训。
5. GPU/独占资源的仲裁只能 fail-open（探测失败默认放行），且给的是某一时刻的快照，不是用完为止的租约承诺——继承旧项目问题41的教训。
6. **不产生游离进程**：插件禁用/卸载必须能把自己启动的子进程干净收掉。旧项目 Flet GUI"关不干净"是前车之鉴（见 LESSONS.md 第6条），这也是这次决定不迁移 Flet GUI、只保留 pywebview 方案的部分原因。
7. **核心和 `in_process` 插件永远装在本项目自己隔离的虚拟环境里，不碰系统 Python/全局 site-packages/全局 PATH**；`subprocess_service` 插件自带独立环境，互相之间、和核心之间都不共享；卸载=删文件夹或跑卸载程序，不留孤儿进程/注册表项/计划任务。
8. 单例扩展点的"切换用哪个实现"必须是配置层面的操作，不需要重启、不需要重新安装（见上节"插件规则"）。
9. 敏感值（API Key 等）错误信息只含类型与摘要，绝不透传上游原文、绝不进日志。

## 依赖独立与主机环境隔离（架构红线7的展开）

**不依赖**：不要求用户预装 Python/CUDA/Node 等运行环境，安装包/便携版自带一切默认插件集需要的东西。

**不污染**（同等重要，容易被忽略）：
- 所有 Python 依赖装进本项目自己隔离的环境，不碰系统 Python、不碰全局 site-packages
- 不修改全局 PATH/环境变量（Windows 安装包例外：只注册自己的开始菜单快捷方式和卸载入口，卸载时清理干净）
- 所有数据/缓存/模型权重落在本项目自己的 `data/` 目录下，不写到用户机器上其他共享位置
- 卸载=删文件夹或跑卸载程序，不留孤儿进程、注册表项、计划任务
- 这条约束在 Phase 1 验收时要能被验证（比如：装/卸载前后对比系统关键位置无变化），不是只写在文档里的口号

## 这个项目不是什么

- 不是旧 `obsidian-rag` 项目的分支或渐进式重构——是全新代码库，不 import、不依赖旧项目任何一行代码
- 不是要做一个真正的在线插件市场/应用商店——v1 插件发现只扫描本地 `plugins/` 目录，用户手动获取插件文件夹
- 不是要把 GPU 仲裁、Agent 写权限门禁做成插件——这两个是核心服务，理由见上文"两个核心组件"节

## 给 AI 的具体规则

1. **改动前先看"数据流铁律"和"架构红线"**，插件之间不能互相直接调用，跨插件数据必须走核心定义的契约类型
2. **新插件=新建 `plugins/<plugin_id>/` 文件夹**，不要把新功能塞进已有插件或核心目录里
3. **核心（`core/`）改动前必须先跟操作者确认**——这是所有插件共同的地基，改错了影响面最大
4. **涉及主机环境隔离的改动要格外小心**：任何会往系统 Python/全局 PATH/注册表写东西的代码，写之前要能说清楚"为什么必须这样、能不能收进自己的隔离目录"，不允许静默引入全局依赖
5. **不确定某个能力该做成插件还是核心服务时**，判断标准是"是否需要在互不知情的插件间做裁判"，拿不准就停下来问操作者，不要自己扩大"核心"的范围
6. **测试纪律沿用旧项目验证过的模式**：新功能带测试用例、索引类集成测试用隔离环境（不碰真实数据）、外部 HTTP 用注入假模块测试——具体测试基建 Phase 0 搭建

## 环境

- Windows 为主战场，深度适配是硬性要求；Linux 用于开发（venv/pip 即可，不要求打包）
- 依赖分两层：`jieba chromadb pymupdf4llm python-docx pywebview mcp datasketch` 是轻量依赖；`torch sentence-transformers`（懒加载，见 official-embedder-bge-m3/official-reranker 两个插件的模块 docstring）体积大，只有真的要跑通真实检索（而不是跑测试）才需要装
- `.venv/bin/python tests/run.py`——全量测试，不需要装 torch/sentence-transformers（全程注入假模型）
- `.venv/bin/python gui_main.py`——打开桌面界面；`.venv/bin/python mcp_stdio.py`——启动 MCP stdio 服务（给 AI 工具接）
- `.venv/bin/python tools/migrate_libraries_json.py <旧libraries.json路径> --dry-run`——旧项目库配置迁移预览

## 提交与文档

- 提交信息中文，`feat:/fix:/docs:/plan:` 前缀
- 未经操作者要求不 push 远程
- 拿不准的架构决策：停下问操作者，不要自行扩大范围

## 参考文档（同一批规则的展开版，不是"规则实际在那边"）

- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)——两个核心组件更详细的设计与理由
- [docs/PLUGIN_SPEC.md](docs/PLUGIN_SPEC.md)——插件清单字段、生命周期时序图完整定义
- [docs/DATA_FLOW.md](docs/DATA_FLOW.md)——数据流管道图与读写权限表
- [docs/ROADMAP.md](docs/ROADMAP.md)——分阶段验收标准（进度记录，不是规范）
- [docs/FEATURE_TRIAGE.md](docs/FEATURE_TRIAGE.md)——旧能力→新插件映射
- [docs/LESSONS.md](docs/LESSONS.md)——旧项目血泪教训精炼版
- [docs/legacy/](docs/legacy/)——旧项目记录性文档原文归档
