# AGENTS.md — RAG REDO 项目协作规范

> **[强制] AGENTS.md ⇄ CLAUDE.md 双文件同步规则**（沿用自参考模板项目验证过的做法）
> 本文件有两个内容必须逐字一致的副本：`AGENTS.md`（供 Codex/AGENTS 体系读取）和 `CLAUDE.md`（供 Claude Code 体系读取）。任何 AI 改动本文件时，必须同时把完全相同的改动写入另一个文件，并在同一次 commit 里提交；改动前先读另一个文件确认没有未同步的差异。两个文件只在这条同步说明块里各自说明"自己是哪一个"，其余正文必须完全相同。

## 项目一句话

Obsidian 笔记本地语义检索系统的完全重构：插件化架构，核心只有插件运行时+数据流管理器两个组件，其余一切（检索算法、embedding 模型、OCR、GUI、MCP 工具）都是插件。目标用户包含完全不懂编程的人，安装必须是"下载→双击→能用"。

## 当前状态

**规划阶段**（2026-09-22 建仓）。核心代码尚未开始写，当前仓库内容是架构规划文档。四份必读文档，按顺序：

1. [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) —— 两个核心组件是什么、为什么
2. [docs/PLUGIN_SPEC.md](docs/PLUGIN_SPEC.md) —— 插件怎么写、怎么被发现加载
3. [docs/DATA_FLOW.md](docs/DATA_FLOW.md) —— 数据流单向管道规则，硬性，违反=架构事故
4. [docs/ROADMAP.md](docs/ROADMAP.md) —— 分阶段验收标准，当前在 Phase 0

其他参考：[docs/FEATURE_TRIAGE.md](docs/FEATURE_TRIAGE.md)（旧能力→新插件映射提案，含3处待操作者拍板的判断）、[docs/LESSONS.md](docs/LESSONS.md)（旧项目血泪教训精炼版）、[docs/legacy/](docs/legacy/)（旧项目记录性文档原文归档，含54个问题编号的完整开发史）。

## 这个项目不是什么

- 不是旧 `obsidian-rag` 项目的分支或渐进式重构——是全新代码库，不 import、不依赖旧项目任何一行代码
- 不是要做一个真正的在线插件市场/应用商店（已确认，见 ROADMAP.md）——v1 插件发现只扫描本地 `plugins/` 目录，用户手动获取插件文件夹
- 不是要把 GPU 仲裁、Agent 写权限门禁做成插件——这两个是核心服务，原因见 ARCHITECTURE.md 2.1/2.2节

## 给 AI 的具体规则（Phase 0 代码落地后逐步补充）

1. **改动前先看 DATA_FLOW.md 的5条禁止事项**，插件之间不能互相直接调用，跨插件数据必须走核心定义的契约类型
2. **新插件=新建 `plugins/<plugin_id>/` 文件夹**，不要把新功能塞进已有插件或核心目录里
3. **核心（`core/`）改动前必须先跟操作者确认**——这是所有插件共同的地基，改错了影响面最大
4. **不确定某个能力该做成插件还是核心服务时，先看 ARCHITECTURE.md 2.1/2.2节的判断标准（是否需要在互不知情的插件间做裁判）**，拿不准就停下来问操作者，不要自己扩大"核心"的范围
5. **测试纪律沿用旧项目验证过的模式**：新功能带测试用例、索引类集成测试用隔离环境（不碰真实数据）、外部 HTTP 用注入假模块测试——具体测试基建 Phase 0 搭建

## 环境

- Windows 为主战场，深度适配是硬性要求；Linux 用于开发（venv/pip 即可，不要求打包）
- 具体命令待 Phase 0 代码落地后补充（目前仓库没有可运行代码）

## 提交与文档

- 提交信息中文，`feat:/fix:/docs:/plan:` 前缀
- 未经操作者要求不 push 远程
- 拿不准的架构决策：停下问操作者，不要自行扩大范围——参考 ROADMAP.md"开放问题"章节里已经标出的几处待确认判断
