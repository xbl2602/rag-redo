id = "official-extractor-docx"
name = "DOCX 提取器"
version = "0.1.0"
api_version = ">=0.1,<0.2"

[provides]
"extractor:docx" = "multi"

[requires]

[runtime]
kind = "in_process"
entry = "official_extractor_docx.plugin:DocxExtractorPlugin"

[permissions]
filesystem = ["vault_read"]
network = false
gpu = false
