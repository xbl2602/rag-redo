id = "official-chunker"
name = "标题感知切块器"
version = "0.1.0"
api_version = ">=0.1,<0.2"

[provides]
chunker = "singleton"

[requires]

[runtime]
kind = "in_process"
entry = "official_chunker.plugin:ChunkerPlugin"

[permissions]
network = false
gpu = false
