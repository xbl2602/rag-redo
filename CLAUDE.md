# CLAUDE.md — RAG REDO 项目协作规范

> **本文件是项目规范，不是进度报告。** 进度、已知缺口和下一步只记录在 `docs/ROADMAP.md`；行为兼容映射和自动门禁记录在 `docs/behavior_contract.json` 与 `tests/test_agents_contract.py`。
>
> **AGENTS.md / CLAUDE.md 同步是强制要求。** 两个文件的项目正文必须完全一致；只有文件身份说明可以不同。修改任一文件前先读另一份，修改后必须同步验证。

## 1. 项目使命与不可变边界

RAG REDO 是 `obsidian-rag` 的插件化重构：

- **只重构底层架构，不重新设计产品功能、行为、界面和边界语义。**
- 内部实现可以换成插件、编排层、独立服务或新的数据结构。
- 对用户可观察的结果必须默认保持旧系统语义：输入、默认值、排序、输出、副作用、失败方式、重试时机、数据可见性和资源生命周期都不能静默改变。
- 旧项目找不到对应行为时，不得自行猜测；先停下向操作者确认，并把它登记为新的行为决策。
- 目标用户不需要手动安装 Python/CUDA/Node 或输入命令；Windows 是最终用户主战场，Linux 用于开发验证。

## 2. 文档与证据优先级

发生冲突时按以下顺序处理：

1. 本文件：不可违反的协作和架构规则。
2. `docs/behavior_contract.json`：逐条行为契约、旧项目证据、当前实现和测试映射。
3. `obsidian-rag` 当前实际代码：产品行为和边界语义的参考源。
4. 行为测试和固定 fixture：可重复验证的外部行为。
5. `docs/ROADMAP.md`：进度、缺口和计划，不是规范。
6. `docs/ARCHITECTURE.md`、`docs/PLUGIN_SPEC.md`、`docs/DATA_FLOW.md`：展开说明；不能覆盖本文件或行为契约。

代码已经偏离规范时，默认是代码缺陷，不能通过修改规范把现状合理化。任何有意偏离必须先获得操作者明确批准，并记录原因、风险、替代行为和对应测试。

## 3. 严格行为兼容协议

### 3.1 什么叫“行为一致”

行为兼容至少包含以下可观察结果：

- 默认配置和优先级；
- 文件是否进入索引、显示或被 Agent 读取；
- 提取路由、缓存命中、失败原因和重试时机；
- 增量/全量索引触发条件；
- 排序、分数、置信度、父子块、正文展开和结果数量；
- MCP/GUI/CLI 的返回值、错误形状和副作用；
- 双链、正文、去重、图谱、WEMM 和导入导出结果；
- 进程、锁、GPU、模型和用户数据的生命周期。

内部类名、模块布局、存储实现和进程模型可以改变；只要外部行为一致，就不算偏离。

### 3.2 行为契约

所有影响旧系统行为的工作必须引用一个稳定的 `BC-*` ID。当前契约见 `docs/behavior_contract.json`，至少覆盖：

- `BC-01` 混合 PDF 路由；
- `BC-02` Agent 格式授权；
- `BC-03` 搜索前自动同步；
- `BC-04` 失败终态、deferred 和重试；
- `BC-05` DataStore 持久化边界；
- `BC-06` 插件隔离和权限；
- `BC-07` small-to-big、正文折叠和结果封顶；
- `BC-08` 自适应结果建议；
- `BC-09` Graph 读模型；
- `BC-10` 导入导出等价性；
- `BC-11` 跨进程资源仲裁；
- `BC-12` 模型/Provider 热切换和版本失效；
- `BC-13` 规范与门禁自身的一致性；
- `BC-14` HyDE 低置信查询增强。

没有对应 `BC-*` 的新功能不得直接进入实现；先补契约、旧项目证据和验收标准。

### 3.3 偏离处理

以下情况必须暂停并向操作者确认：

- 旧项目没有对应能力，但需要新设计；
- 为了插件化而改变用户可见行为；
- 为降低复杂度而删除旧能力；
- 为满足新架构而改变失败、重试、权限或数据生命周期；
- 任何无法用旧代码和测试证明的“等价”判断。

确认后必须把偏离写入行为契约，不能只在提交信息或代码注释里说明。

## 4. 核心架构边界

### 4.1 两个核心职责

核心只有两个职责边界，不限制为两个 Python 文件：

1. **插件运行时**：发现、校验、加载、启停、扩展点注册、配置激活和资源仲裁。
2. **数据流/契约管理器**：管道顺序、公共数据契约、持久化所有权、写入门禁和跨插件协调。

GUI、CLI、MCP、索引器、提取器、模型、存储、OCR、视觉检索和外部服务都通过核心公共 API 接入，不在入口中重新拼业务顺序。

### 4.2 插件运行时

- 插件目录必须有合法 `plugin.toml` 和稳定的 API 版本。
- `on_load → on_enable` 和 `on_disable → on_unload` 必须有明确边界。
- 插件发现、加载、启用失败必须可诊断，失败插件不能拖垮宿主。
- 单例扩展点必须有显式 active choice；多值扩展点必须有明确聚合规则。
- 切换 active choice 不能要求重新安装；新 worker 必须拿到同一份配置快照。
- 模型、chunker、存储等版本变化必须能触发受控的索引失效或重建。

### 4.3 `in_process` 与 `subprocess_service`

- `in_process` 是**受信任插件**：与核心共享环境，不宣称能抵御恶意代码；禁止跨插件 import，禁止绕过公共契约。
- `subprocess_service` 必须拥有真实进程边界、独立解释器/环境和明确退出协议。
- 重依赖、独占 GPU、独立模型或版本冲突能力不能无理由放进核心环境。
- 核心和插件不得修改系统 Python、全局 site-packages、全局 PATH 或其他应用的数据目录。
- 子进程必须能被停止、等待、回收；不能留下端口、进程树或锁残留。

### 4.4 DataStore 和持久化

- DataStore 是持久化所有权、命名空间和权限的唯一网关。
- 非存储插件不得自行拼接或打开 Chroma、SQLite、JSON、缓存、日志和索引路径。
- 存储插件只能使用 DataStore 发放的 namespace/handle；不能把 DataStore 设计成理解所有后端查询语义的大一统数据库。
- 配置、索引、缓存、失败记录、关系、摘要和插件状态都必须有明确的所有者和生命周期。
- 原子发布必须覆盖“搜索可见状态”；普通配置和缓存写入也必须避免半截 JSON、截断文件或丢失更新。

### 4.5 数据流和契约

- 索引态固定为：选择 → 提取 → 清洗/切块 → 向量化/词法化 → 存储 → 后处理。
- 查询态固定为：查询解析 → 多路召回 → 融合 → 重排 → 标注/建议 → 响应。
- 同一业务判断只能有一个权威实现。
- 跨插件数据必须使用 `core.contracts` 中定义的类型；不能靠裸字典、字符串字段名和隐含约定通信。
- 任何新增扩展点都必须定义最小接口、失败语义、版本和测试 fixture。

## 5. 索引和提取硬规则

- `collect`/文件枚举只能有一个权威入口，所有索引、WEMM、去重、图谱和 GUI 状态都必须复用它。
- 路径显式纳入/排除、目录排除、格式规则和 Agent 授权的优先级必须与 `BC-*` 契约一致。
- 混合 PDF 不得产生“文字页已收、图片页静默丢失”的半份结果。
- 提取器失败必须折叠成结构化失败结果，不能把普通文件失败抛成宿主异常。
- `unreadable`、`empty`、`tbd`、`scanned`、`extract-failed` 等终态不能合并成一个无法诊断的 `failed`。
- 稳定失败在输入和能力签名未变化时不能每轮重复消耗资源；能力变化或内容变化时必须能重试。
- 瞬态服务不可用必须与永久失败区分，不能伪装成稳定终态。
- 修改提取、清洗、切块、嵌入、视觉渲染或路由逻辑，必须更新对应版本并使旧数据失效。
- 每轮索引必须先写独立 generation，完成后原子发布；停止、异常或崩溃不能把半成品切换成当前索引。
- 成功提交后必须回收不再被引用的 generation、缓存、临时文件和集合。

## 6. 检索和结果硬规则

- 文字检索默认必须保持旧系统的 dense/BM25/RRF/reranker 语义。
- 视觉检索是独立的第二系统，不得与文字向量空间或分数混合。
- 置信度必须使用明确尺度，不得用批内归一化制造虚假的跨查询可比性。
- `small-to-big`、同节折叠、同文件封顶、list/body 模式和过滤后补位必须由行为契约逐项验证。
- 结果建议必须是纯规则、零副作用、最多返回限定数量，并与结果本体使用不同协议通道。
- 搜索前 freshness 必须保持旧系统语义：发现变化时自动同步，首跑等待，非空库可后台更新，不能让搜索悄悄使用过期数据。
- GUI、MCP、CLI 必须调用同一业务服务层；入口不得各自实现排序、过滤或错误处理。

## 7. 资源和进程硬规则

- 同一独占资源必须有明确 holder、优先级、获取、释放、抢占和进程死亡处理。
- 锁只能说明当前状态，不能代替真实的跨进程 holder 协调。
- “探测失败”与“所有权未知”必须区分：探测可 fail-open，所有权未知不能直接宣称资源空闲。
- BGE、reranker、WEMM、MinerU 的模型加载、卸载、抢占和失败降级必须有可验证的时序。
- 核心重载、GUI/MCP/worker 同时运行时，不得重复加载模型或重复占用 GPU。
- 任何子进程、后台任务、文件锁和模型线程都必须有停止、等待和异常收口路径。
- 敏感值只允许出现在用户配置或必要的进程环境中，不得进入日志、异常文本、归档元数据或测试输出。

## 8. 变更流程

每个行为或架构改动必须按以下顺序执行：

1. 找到对应 `BC-*`；没有就新增契约并确认范围。
2. 读取旧项目对应生产代码，记录 `legacy_ref`，不能只读旧文档。
3. 写清楚当前实现的外部行为差异和预期保持的兼容点。
4. 先写复现测试或固定 fixture，再实现。
5. 只改完成该契约所需的最小范围。
6. 运行对应测试、统一测试入口和规范门禁。
7. 更新行为契约状态和 `docs/ROADMAP.md`，不能把未验证项写成完成。
8. 若发现需要偏离旧行为，停止并向操作者确认。

核心目录、公共契约、DataStore、入口、打包和资源仲裁的修改尤其需要明确确认。

## 9. 测试与验收

- 新功能必须有测试；bug 修复必须先有复现用例。
- 索引集成测试使用临时数据目录和假模型/假 HTTP，不触碰真实 Vault、Chroma 或生产缓存。
- 每个 `BC-*` 必须至少有一个 `test_refs`；没有测试引用的契约不能标记为 `pass`。
- `blocked` 契约可以暂时没有完整实现测试，但必须保留明确的 blocker 和旧项目证据。
- 统一测试入口必须包含规范门禁；门禁失败不能被普通测试数量掩盖。
- 测试通过只证明测试覆盖的行为通过，不代表所有行为兼容；最终状态以行为契约、测试和操作者确认共同决定。
- 不得为了让测试变绿而放宽断言、删除测试、修改旧行为或把失败标成预期通过。

## 10. 环境与提交

Windows 开发命令：

```powershell
$env:PYTHONIOENCODING = "utf-8"
.\.venv\Scripts\python.exe tests\run.py
```

POSIX 开发命令：

```bash
PYTHONIOENCODING=utf-8 .venv/bin/python tests/run.py
```

- 未经操作者要求不 push。
- 不提交凭据、真实 Vault、模型缓存、用户数据、索引数据库或临时测试数据。
- 提交信息使用中文和 `feat:/fix:/docs:/test:/plan:` 前缀。
- `AGENTS.md` 与 `CLAUDE.md` 必须同步修改；同步检查由 `tests/test_agents_contract.py` 执行。

## 11. 行为门禁入口

规范同步和契约元数据由以下命令验证：

```powershell
$env:PYTHONIOENCODING = "utf-8"
.\.venv\Scripts\python.exe tests\test_agents_contract.py
```

完整回归由以下命令执行：

```powershell
$env:PYTHONIOENCODING = "utf-8"
.\.venv\Scripts\python.exe tests\run.py
```

测试门禁只验证规范和契约的可执行性；它不会自动证明业务行为已经兼容。未完成的 `BC-*` 必须保持 `blocked`，直到实现、测试和操作者确认完成。
